<!--
Personal, machine-local overrides for this repository. Claude Code reads this file
alongside CLAUDE.md, and what it says here wins for you alone.

MUST be gitignored. This file exists so one person can record local paths, personal
preferences and machine-specific quirks WITHOUT imposing them on the team. Committing
it defeats the entire point and pushes your local setup onto everyone else.

Add to .gitignore:
    CLAUDE.local.md
-->

# Local overrides — {{YOUR_NAME}}

Personal notes for working in this repository on this machine. Not committed.

## Local paths and environment

| What | Where on this machine |
| --- | --- |
| {{THING}} | `{{LOCAL_PATH}}` |

## Local commands

Commands that differ from the ones in `CLAUDE.md` because of this machine's setup:

```bash
{{LOCAL_COMMAND}}
```

## Working preferences

{{HOW_YOU_WANT_CLAUDE_TO_WORK_HERE — verbosity, whether to run tests unprompted,
which parts of the codebase you want left alone}}

## Scratch notes

{{IN_FLIGHT_CONTEXT — what you are mid-way through, so a new session picks it up}}
