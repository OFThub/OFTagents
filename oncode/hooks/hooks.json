{
  "description": "oncode — one UserPromptSubmit entry point for two independent switches: ideal-prompt routes the submitted prompt through the token-trajectory optimizer, lean-reply shapes the answer written back to the user.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/prompt-mode.mjs\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
